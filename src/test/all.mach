# the test-only modules no executable entry reaches: `mach test . --lib tests` runs their tests

use std.runtime;

use hedge.test.budget;
use hedge.test.lanes;
use hedge.test.laurel;
use hedge.test.runtime.caching;
use hedge.test.runtime.composition;
use hedge.test.runtime.files;
use hedge.test.runtime.lifecycle;
use hedge.test.runtime.protocol;
use hedge.test.runtime.proxying;
use hedge.test.runtime.serving;
use hedge.test.runtime.telemetry;
use hedge.test.runtime.tests;
