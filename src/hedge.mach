# hedge product identity

use std.types.bool.bool;
use std.types.bool.false;
use std.types.string.str;

pub val VERSION: str = "0.5.1";

# report whether the serving runtime is implemented
pub fun implementation_ready() bool {
    ret false;
}

test "hedge: scaffold reports not ready" {
    if (implementation_ready()) { ret 1; }
    ret 0;
}
